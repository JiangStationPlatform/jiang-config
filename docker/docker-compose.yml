version: '3.1'
services:
  jiang-config:
    restart: always
    image: jiang-config
    container_name: jiang-config
    ports:
      - 8769:8769
    networks:
      - config_network
networks:
  config_network: